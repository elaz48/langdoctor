# CLAUDE.md

Guidance for Claude Code when working in this repository.

## Project

**langdoctor** — _one-line description TBD._ MIT licensed (© 2026 elaz48).

Status: early scaffolding — no application code or stack chosen yet.

## Commands

No build/test/lint tooling is configured yet. Fill these in once a stack is picked.

| Task | Command |
|------|---------|
| Build | _TBD_ |
| Test | _TBD_ |
| Lint / typecheck | _TBD_ |
| Dev server | _TBD_ |

## Conventions

- _Add project-specific conventions here as they emerge (structure, naming, patterns)._

## Notes

- The ECC harness is installed locally under `.claude/` (git-ignored, not committed).
- Re-run `/project-init` once a stack manifest exists (e.g. `package.json`, `pyproject.toml`, `go.mod`) to layer language-aware rules and skills onto the baseline.
